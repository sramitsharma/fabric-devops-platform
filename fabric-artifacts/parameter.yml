# fabric-cicd parameter.yml — ONLY for what Variable Libraries can't hold
# (workspace-scoped GUIDs baked into item definitions). Everything else
# (connection strings, paths, thresholds) lives in the Variable Library.
find_replace:
  - find_value: "11111111-1111-1111-1111-111111111111"   # dev bronze lakehouse id
    replace_value:
      test:    "22222222-2222-2222-2222-222222222222"
      preprod: "33333333-3333-3333-3333-333333333333"
      prod:    "44444444-4444-4444-4444-444444444444"
    item_type: "Notebook"
